<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Doctor Verification</title>
    <link rel="stylesheet" href="/css/adminDashboard.css">
</head>
<body>
    <div class="container">
        <h1>Admin Panel - Doctor Verification</h1>

        <div class="search-bar">
            <input type="text" id="search" placeholder="Search accepted doctor by name or ID...">
            <button onclick="searchAcceptedDoctor()">Search</button>
        </div>

        <h2>Pending Doctors</h2>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Specialization</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="pendingDoctorTable">
                <!-- Example pending row -->
                <tr>
                    <td>101</td>
                    <td>Dr. Jamal</td>
                    <td>jamal@just.edu.bd</td>
                    <td>Cardiologist</td>
                    <td>Pending</td>
                    <td>
                        <button class="btn" onclick="verifyDoctor(101)">Verify</button>
                        <button class="btn btn-secondary" onclick="rejectDoctor(101)">Reject</button>
                    </td>
                </tr>
                <!-- Add more pending rows dynamically -->
            </tbody>
        </table>

        <h2 id="searchResultsHeader" style="display: none;">Search Results</h2>
        <table id="searchResultsTable" style="display: none;">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Specialization</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- Search results will appear here dynamically -->
            </tbody>
        </table>
    </div>

    <script>
        // function verifyDoctor(doctorId) {
        //     console.log(`Verify button clicked for doctor with ID ${doctorId}`);
        //     alert(`Doctor with ID ${doctorId} verified.`);
        //     // Implement API call or functionality to verify doctor here
        // }

        // function rejectDoctor(doctorId) {
        //     console.log(`Reject button clicked for doctor with ID ${doctorId}`);
        //     alert(`Doctor with ID ${doctorId} rejected.`);
        //     // Implement API call or functionality to reject doctor here
        // }

        // function searchAcceptedDoctor() {
        //     const searchValue = document.getElementById('search').value.toLowerCase();
        //     console.log(`Search initiated for accepted doctors with query: "${searchValue}"`);

        //     const resultsTable = document.getElementById('searchResultsTable');
        //     const resultsHeader = document.getElementById('searchResultsHeader');
        //     const resultsBody = resultsTable.querySelector('tbody');

        //     // Clear previous results
        //     resultsBody.innerHTML = '';

        //     // Simulate fetching accepted doctors (replace with actual API call if necessary)
        //     const acceptedDoctors = [
        //         { id: 201, name: 'Dr. Shamim', email: 'shamim@just.edu.bd', specialization: 'Dermatologist', status: 'Accepted' },
        //         { id: 202, name: 'Dr. Arobindo', email: 'arobindo@just.edu.bd', specialization: 'Pediatrician', status: 'Accepted' }
        //     ];

        //     const filteredDoctors = acceptedDoctors.filter(doctor =>
        //         doctor.name.toLowerCase().includes(searchValue) ||
        //         doctor.id.toString().includes(searchValue)
        //     );

        //     if (filteredDoctors.length > 0) {
        //         resultsHeader.style.display = 'block';
        //         resultsTable.style.display = 'table';

        //         filteredDoctors.forEach(doctor => {
        //             const row = document.createElement('tr');

        //             row.innerHTML = `
        //                 <td>${doctor.id}</td>
        //                 <td>${doctor.name}</td>
        //                 <td>${doctor.email}</td>
        //                 <td>${doctor.specialization}</td>
        //                 <td>${doctor.status}</td>
        //                 <td><button class="btn btn-secondary" onclick="removeSearchResult(${doctor.id})">Remove</button></td>
        //             `;

        //             resultsBody.appendChild(row);
        //         });
        //     } else {
        //         alert('No matching accepted doctors found.');
        //         resultsHeader.style.display = 'none';
        //         resultsTable.style.display = 'none';
        //     }
        // }

        // function removeSearchResult(doctorId) {
        //     console.log(`Remove button clicked for search result with ID ${doctorId}`);
        //     const row = Array.from(document.querySelectorAll('#searchResultsTable tbody tr')).find(tr =>
        //         tr.cells[0].innerText == doctorId
        //     );

        //     if (row) {
        //         row.remove();
        //         console.log(`Doctor with ID ${doctorId} removed from search results.`);

        //         // Hide results table if empty
        //         const resultsBody = document.getElementById('searchResultsTable').querySelector('tbody');
        //         if (resultsBody.children.length === 0) {
        //             document.getElementById('searchResultsHeader').style.display = 'none';
        //             document.getElementById('searchResultsTable').style.display = 'none';
        //         }
        //     }
        // }
    </script>
</body>
</html>
